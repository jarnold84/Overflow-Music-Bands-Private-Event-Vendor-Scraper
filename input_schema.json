{
  "title": "Overflow Universal Lead Scraper",
  "description": "Input schema for campaign-specific scraping of websites to extract lead intelligence and messaging signals.",
  "type": "object",
  "schemaVersion": 1,
  "properties": {
    "startUrls": {
      "title": "Start URLs",
      "type": "array",
      "description": "List of URLs to begin scraping from. These should be relevant to the campaign (e.g. vendor directories, faculty pages, retreat listings).",
      "items": {
        "type": "string",
        "format": "uri"
        "anyOf": [
          { "type": "string", "format": "uri" },
          {
            "type": "object",
            "properties": {
              "url": { "type": "string", "format": "uri" }
            },
            "required": ["url"]
          }
        ]
      },
      "minItems": 1
    },
    "campaignMode": {
      "title": "Campaign Mode",
      "type": "string",
      "description": "Which type of campaign this run supports. Determines classification and routing logic.",
      "default": "universal",
      "enum": [
        "universal",
        "wedding",
        "corporate",
        "mixed"
      ]
    },
    "maxConcurrency": {
      "title": "Max Concurrency",
      "type": "integer",
      "description": "Maximum number of concurrent pages crawled. Defaults to 10.",
      "default": 10,
      "minimum": 1
    },
    "proxyConfiguration": {
      "title": "Proxy Configuration",
      "type": "object",
  },
  "required": ["startUrls"]
}
